#include "ToyFactory.h"
#include <Windows.h>

/*
int main()
{
	int toy_code;
	while (1)
	{
		std::cout << "Enter the toycode for production: ";
		std::cin >> toy_code;
		if (!toy_code)
			break;

		Toy* toy = ToyFactory::CreateToy(toy_code);
		if (toy)
		{
			toy->ShowProduct();
			delete toy;
		}
	}
	std::cout << "Exit...\n";
	Sleep(5000);
	return 0;
}
 */